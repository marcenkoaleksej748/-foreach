 public function printTree() {
        echo "\nGit Repository:\n";
        foreach ($this->branches as $branch => $commits) {
            echo "  $branch: " . implode(', ', $commits) . "\n";
        }
